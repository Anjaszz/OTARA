import { DisplayOrderWeightPipe } from './display-order-weight.pipe';

describe('DisplayOrderWeightPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayOrderWeightPipe();
    expect(pipe).toBeTruthy();
  });
});
