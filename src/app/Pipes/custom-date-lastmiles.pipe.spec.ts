import { CustomDateLastmilesPipe } from './custom-date-lastmiles.pipe';

describe('CustomDateLastmilesPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomDateLastmilesPipe();
    expect(pipe).toBeTruthy();
  });
});
