import { DiplayBindingLastmilesPipe } from './diplay-binding-lastmiles.pipe';

describe('DiplayBindingLastmilesPipe', () => {
  it('create an instance', () => {
    const pipe = new DiplayBindingLastmilesPipe();
    expect(pipe).toBeTruthy();
  });
});
