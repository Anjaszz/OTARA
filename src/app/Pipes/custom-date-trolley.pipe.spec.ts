import { CustomDateTrolleyPipe } from './custom-date-trolley.pipe';

describe('CustomDateTrolleyPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomDateTrolleyPipe();
    expect(pipe).toBeTruthy();
  });
});
